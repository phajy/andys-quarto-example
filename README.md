# Quarto Example

An example of my Quarto workflow for lecture notes.

A quick start for your own project is to copy the `_quarto.yml`, `styles.css`, and the `index.qmd` files to a new directory and then add your own content.